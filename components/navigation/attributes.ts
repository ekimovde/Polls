import { getTestSelectors } from '~/shared/utils/unit-test/test-id';

export const COMPONENT_NAME = 'navigation-block';

export enum NavigationBlockTextAttribute {
  //
}

export enum NavigationBlockTestLocator {
  //
}

export const dtNavigationBlock = getTestSelectors(COMPONENT_NAME, NavigationBlockTestLocator);
