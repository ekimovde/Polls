import { getTestSelectors } from '~/shared/utils/unit-test/test-id';

export const COMPONENT_NAME = 'changelog-block';

export enum ChangelogBlockTextAttribute {
  //
}

export enum ChangelogBlockTestLocator {
  //
}

export const dtChangelogBlock = getTestSelectors(COMPONENT_NAME, ChangelogBlockTestLocator);
