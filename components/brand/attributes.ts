import { getTestSelectors } from '~/shared/utils/unit-test/test-id';

export const COMPONENT_NAME = 'brand-block';

export enum BrandBlockTextAttribute {
  //
}

export enum BrandBlockTestLocator {
  //
}

export const dtBrandBlock = getTestSelectors(COMPONENT_NAME, BrandBlockTestLocator);
