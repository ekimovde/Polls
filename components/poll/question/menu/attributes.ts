import { getTestSelectors } from '~/shared/utils/unit-test/test-id';

export const COMPONENT_NAME = 'poll-question-menu';

export enum PollQuestionMenuTextAttribute {
  //
}

export enum PollQuestionMenuTestLocator {
  //
}

export const dtPollQuestionMenu = getTestSelectors(COMPONENT_NAME, PollQuestionMenuTestLocator);
