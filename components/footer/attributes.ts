import { getTestSelectors } from '~/shared/utils/unit-test/test-id';

export const COMPONENT_NAME = 'footer-block';

export enum FooterBlockTextAttribute {
  //
}

export enum FooterBlockTestLocator {
  //
}

export const dtFooterBlock = getTestSelectors(COMPONENT_NAME, FooterBlockTestLocator);
